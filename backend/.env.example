# Copy to .env and fill in values
PORT=5000

DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=3035@Devil
DB_NAME=postgres
DB_POOL_LIMIT=10

# Optional: restrict CORS (default allows all). Example: http://localhost:5173
# CORS_ORIGIN=http://localhost:5173
